import { SpeakeasyBase, SpeakeasyMetadata } from "../../../internal/utils";

export enum ConnectionTypeEnum {
  Snowflake = "snowflake",
  Bigquery = "bigquery",
  Redshift = "redshift",
  Mongodb = "mongodb",
  Webhook = "webhook",
}
